# HelpDesk CRM - Медицинская Система Поддержки

Современная система управления заявками (Help Desk) и ресурсами, специально разработанная для медицинских организаций.

## 🚀 Технологический стек

- **Frontend**: React 19, TypeScript, Vite
- **Стилизация**: Tailwind CSS, Shadcn UI
- **Управление состоянием**: Zustand
- **Маршрутизация**: React Router 7
- **Формы и валидация**: React Hook Form, Zod
- **Иконки**: Lucide React
- **Работа с датами**: date-fns

## 🛠 Основные модули

### 🎫 Заявки (Tickets)
- Создание и редактирование заявок на техническую поддержку.
- Назначение исполнителей (администраторов или техников).
- Комментирование заявок для обсуждения проблем.
- Фильтрация по статусам (Новая, В работе, Решена) и приоритетам.

### 📄 Документы (Documents)
- Управление медицинскими и административными документами.
- Календарное представление сроков действия документов.
- Категоризация и отслеживание статусов документов.

### 📦 Инвентаризация (Inventory)
- Учет медицинского оборудования и ИТ-активов.
- Отслеживание состояния и местоположения оборудования.

### 📚 База знаний (Knowledge Base)
- Создание инструкций и руководств для персонала.
- Быстрый доступ к решениям типичных проблем.

### 🔐 Роли и Доступ
- **Администратор**: Полный доступ ко всем модулям и управлению пользователями.
- **Техник**: Работа с заявками и инвентарем.
- **Пользователь**: Создание заявок и просмотр базы знаний.

## 📁 Структура проекта

- `src/api`: Имитация API-сервисов (Mock API).
- `src/app`: Конфигурация приложения, роутинг и глобальные стили.
- `src/components`: Переиспользуемые UI-компоненты (Shadcn UI + кастомные).
- `src/data`: Мок-данные для демонстрации.
- `src/hooks`: Кастомные React-хуки для бизнес-логики.
- `src/pages`: Высокоуровневые компоненты страниц.
- `src/screens`: Детальная реализация интерфейсов страниц.
- `src/stores`: Zustand-хранилища для управления состоянием.
- `src/types`: TypeScript определения типов и интерфейсов.

## 🏁 Начало работы

1. Установите зависимости:
   ```bash
   npm install
   ```

2. Запустите в режиме разработки:
   ```bash
   npm run dev
   ```

3. Соберите проект:
   ```bash
   npm run build
   ```

## 📝 Журнал изменений (Changelog)

### v1.0.8 (Текущая)
- **Календарь (UI/UX)**:
  - Перенос названия месяца в основную шапку (рядом с кнопкой года) для экономии места.
  - Оптимизация высоты строк: теперь текущий месяц занимает основной экран, с показом первой недели следующего месяца.
  - Исправлено перекрытие заголовка месяца "липкой" панелью дней недели (добавлен scroll-offset).
  - Восстановлено отображение названий месяцев внутри сетки календаря по просьбе пользователя.
- **Производительность**:
  - Глубокая оптимизация анимаций селектора даты (переход на `transform`/`opacity` и `will-change`).
  - Улучшена интерактивность кнопок выбора месяца/года (z-index, preventDefault).
  - Динамическая подгрузка месяцев (±2 года от текущей даты) для мгновенного перехода.

### v1.0.7.1 (Подверсия)
- **Документы**: Исправлена ошибка, при которой заголовок месяца "прятался" за верхней панелью при переключении.
- **Интерфейс**: Улучшено позиционирование полноэкранного селектора под новой компактной шапкой.

### v1.0.7
- **Чат**: Контекстное меню, проверка регистрации пользователей, карточки контактов.
- **Справочник**: Отображение полного списка, фиксация шапки, компактный поиск.
- **Документы**: Перенос фильтров в заголовок, цветовые индикаторы событий, переход на полноэкранный селектор даты.
- **Layout**: Глобальная настройка `h-screen` для полноэкранных модулей, улучшение активных состояний навигации.
